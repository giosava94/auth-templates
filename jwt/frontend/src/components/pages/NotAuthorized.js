/* Show this page when the user has signed in but it is forbidden to see the selected page. */
function NotAuthorized(props) {
  return <div>Current user is not authorized to see this page</div>;
}

export default NotAuthorized;
